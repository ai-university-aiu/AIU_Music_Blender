AIU MUSIC BLENDER - DSPARCD PHASE 5 - REFINEMENT - VERSION 18

Date: 2026-08-07

(Version 18 supersedes version 17, now in docs/archive/; it adds Iteration 18.)


HOW REFINEMENT WORKS ON THIS PROJECT

Build in small tested steps; every improvement is recorded here, and every recorded improvement names the test that proves it.

The Golden Rule is enforced as a regression: the classic-mode tests (T1 through T4 of the Specification) must stay green after every change, forever.


ITERATION LOG

Iteration 1 (2026-08-07): First working build.

• The analysis pipeline, beat graph, jukebox walk, renderer, mash-up engine, and all three faces were built to Specification version 1, with the test suite of scenarios T1 through T7.

• Decision refined during building: per-beat features average the frames BETWEEN beats (not at the beat instant), because a beat is a span of sound, not a point; this noticeably improves jump seamlessness on synthetic tests.

• Decision refined during building: jump edges exclude the three nearest neighbor beats on each side, because trivially-adjacent jumps are inaudible non-events that crowd out interesting edges.

• Decision refined during building: the walk's end-of-song fallback jumps to the BEST-MATCHING earlier beat rather than beat zero, so the loop point is musical rather than mechanical.

Iteration 2 (2026-08-07): Web page chaos slider labeling, by owner request.

• The chaos slider on the web page now carries a text label ("Chaos (jump probability):"), a hover tooltip explaining what the probability means, and a live numeric readout that updates as the slider is dragged.

• Engine behavior is unchanged; this is a face-only change, so the Golden Rule regression is untouched by construction, and the web tests (scenario T6) stay green.

Iteration 3 (2026-08-07): Web page control layout, by owner observation.

• The owner saw the chaos label wrap onto a different line from its slider on a narrow screen, separating a label from the thing it labels.

• The controls now stack one per line (song chooser, then the chaos row, then the play and stop buttons), and the chaos label, slider, and live value are held together as one flexible row that centers and wraps as a unit.

• Face-only change again; the web tests (scenario T6) stay green.

Iteration 4 (2026-08-07): Web page information cards, button states, and YouTube input, by owner request.

• Tempo and Key info cards now appear after analysis, showing the detected beats per minute and the detected key with its Camelot code, styled as warm cards under the controls.

• The Play and Stop buttons now say their state: "Play forever" becomes "Playing forever..." while walking, and "Stop" rests as "Stopped"; disabled buttons dim so state is visible at a glance.

• A "YouTube URL:" text box now sits above the file chooser inside the same red group rectangle; pressing Enter or the "Fetch & analyze" button downloads the audio locally (yt-dlp) and analyzes it through the same pipeline.

• The tagline "Nothing to deprecate. Nothing to die." was removed from the page at the owner's request.

• Face-only changes; the web tests (scenario T6) stay green.

Iteration 5 (2026-08-07): The project is renamed AIU_Music_Blender, by owner instruction.

• The GitHub repository was renamed from AI_Music_Blender to AIU_Music_Blender (GitHub redirects the old name), and the local folder was renamed to match.

• Every instance of "AI_Music_Blender", "AI Music Blender", and "AI MUSIC BLENDER" across the documents, code comments, web page, README, and CONSTITUTION now reads AIU; the web page heading now says "AIU Music Blender".

• The lowercase engine package name (aiu_music_blender), the launcher name (aiu-music-blender), and the vendored ancestor snapshot are unchanged: the first two were not part of the instruction and keep every command working, and the vendor snapshot stays pristine by design.

• The full test suite stays green after the rename.

Iteration 6 (2026-08-07): The lowercase names complete the rename, by owner instruction.

• The engine package is renamed ai_music_blender to aiu_music_blender, the launcher is renamed bin/ai-music-blender to bin/aiu-music-blender, and the DSPARCD Fileset document files (current and archived) are renamed to the aiu_music_blender_ prefix, all with git mv so history follows.

• Every import, test, document reference, command example, and the program's own help name were updated to match; the analysis cache folder moved to ~/.cache/aiu_music_blender with its contents preserved, so no song needs re-analysis.

• The vendored EternalJukebox snapshot remains pristine, as always.

• The full test suite (8 tests) and the launcher help both pass after the rename.

Iteration 7 (2026-08-07): The Fetch button now shows its work, by owner bug report.

• The owner pasted a YouTube link, pressed Fetch & analyze, and saw nothing happen; server-side reproduction showed the pipeline itself succeeding (HTTP 200), pointing at two interface faults rather than an engine fault.

• Fault one: a YouTube fetch takes a minute or more, and the only feedback was small status text; the button now disables itself and reads "Fetching..." for the whole round trip, restoring itself afterward.

• Fault two: a network or server failure (for example, a web server process started before the renames and still running dead code) was silent; every failure is now caught and printed on the status line, including a reminder to check that bin/aiu-music-blender web is running.

• Face-only change; the web tests (scenario T6) stay green.


Iteration 9 (2026-08-07): No more cutting mid-vocal-phrase, by owner instruction, after the owner heard jumps chopping words in half.

• Context matching: each pair's similarity distance now blends in the distances of the beats that FOLLOW them (one ahead at weight 0.6, two ahead at weight 0.3), so a jump prefers a landing whose continuation also matches; jumps migrate to phrase boundaries on their own.

• Vocal awareness: the pipeline now measures per-beat vocal activity (harmonic energy in the 200 to 4000 Hertz band, via harmonic-percussive separation), stores it in the analysis record, and penalizes any jump into or out of a strongly singing beat, scaled to each song's own typical distance; the walker cuts on the breath, not on the word.

• Sample-accurate web playback: the web page now schedules every beat on the Web Audio clock with a lookahead pump, instead of the drifting general-purpose timer; the per-beat gaps and smears are gone, and the chaos slider stays live.

• The analysis record format is now version 2 (with the per-beat vocal list); older caches re-analyze automatically on first use, per the versioned-record design in the Architecture.

• Measured on the owner's test song: jump-capable beats average 0.27 vocal activity against 0.34 for all beats - the graph's edges have already moved away from the singing.

• The full test suite (8 tests) stays green.

Iteration 10 (2026-08-07): The two-rectangle web layout, the mash-up controls, and the house font, by owner instruction.

• The web page is now organized as two big rounded-corner group rectangles: "AIU Song Blender" (YouTube input, file chooser, chaos slider, play and stop, with the tempo card, key card, and the beats-and-edges notice moved INSIDE the rectangle), and below it "AIU Mash-Up Blender" (SONG-1 and SONG-2 YouTube inputs, a Blend button, and an honest status line).

• Blending in the web page posts both links to the existing /mashup route; the finished mash-up loads straight into the Song Blender above - cards, arcs, and Play forever - so a blend can immediately play forever.

• The house font, Source Code Pro (Adobe's open-source SIL-licensed face), is vendored into aiu_music_blender/static/fonts/ with its license and served from our OWN server, applied to all text including buttons and inputs; no outside font fetch at page load, per the self-hosting principle.

• The web tests (scenario T6) stay green.

Iteration 11 (2026-08-07): A spinning working indicator on the Blending button, by owner request.

• While a blend runs, the button now shows a small spinning gold ring beside the word "Blending..." - drawn and animated purely with our own page styling (no images, no outside resources), so long blends visibly show the machine is working.

• The button restores its plain label when the blend ends, success or failure; the web tests (scenario T6) stay green.

Iteration 12 (2026-08-07): The spinner joins the Fetch button too, by owner approval.

• The Fetch & analyze button in the Song Blender rectangle now shows the same spinning gold ring beside "Fetching..." while a download and analysis run, restoring its plain label when the work ends.

• Same self-hosted styling as Iteration 11; the web tests (scenario T6) stay green.

Iteration 13 (2026-08-07): Independent per-song vocal checkboxes, by owner instruction, so mash-ups need never have conflicting, overlapping, or simultaneous vocals.

• The mash-up engine now takes an independent vocal choice for each song: both on gives the plain full-song blend; one on gives that song's vocal over the other's Demucs-separated instrumental (overlap impossible by construction); both off gives a karaoke blend of the two instrumentals.

• The AIU Mash-Up Blender rectangle carries a "vocals" checkbox beside each song's link - SONG-1 checked and SONG-2 unchecked by default, the classic one-vocal mash-up - and the honest report now states which vocals are in the blend.

• The older command line --stems flag still works, translated to SONG-1 vocals over SONG-2 instrumental; the engine's report gained vocals_a and vocals_b fields.

• Any choice needing separation requires Demucs and warns about the extra minutes; the full test suite (8 tests) stays green.

Iteration 14 (2026-08-07): The Mash-Up Blender gets its own walk controls, by owner instruction, so one rectangle covers one use.

• The AIU Mash-Up Blender rectangle now carries its own chaos slider with live value, and its own Play Forever and Stop buttons, so a user who blends never has to leave the rectangle to listen.

• Both rectangles drive the ONE shared player: whichever Play Forever is pressed steers the walk with ITS OWN slider (read live while playing), either Stop ends it, and the two button pairs mirror each other's state so the page never shows a contradiction.

• Both rectangles' buttons now read in mixed case: "Play Forever", "Playing Forever...", "Stop", "Stopped".

• Face-only change; the web tests (scenario T6) stay green.

Iteration 15 (2026-08-07): Both-vocals-checked now APPENDS, by owner instruction.

• When both vocal checkboxes are checked, the engine no longer overlays the two full songs; it appends SONG-2 (with vocals) onto the end of SONG-1 (with vocals) - both tempo-conformed to the common tempo and key-aligned - joined by a half-second equal-power seam, making one big long song.

• That long song then feeds the blender like any other: its beat graph spans BOTH songs, so the infinite walk drifts between them through harmonically compatible sections - the vision document's "infinite mash-up radio" in its purest form.

• The report gained a mode field ("append" or "mix"), and the web page says which happened; the overlay behaviors of the other three checkbox combinations are unchanged.

• Proven end to end on synthetic songs (two twenty-second songs became one 39.5-second long song), and the full test suite (8 tests) stays green.

Iteration 16 (2026-08-07): Both-boxes-unchecked now makes the entire track instrumental only, by owner instruction.

• With neither vocal checkbox checked, the engine now Demucs-separates BOTH songs, keeps only their instrumental stems, and appends them end to end - the same one-big-long-song shape as both-boxes-checked, but with no vocals anywhere in the track.

• The append rule is now symmetrical: matching choices (both on, or both off) append one long song - with vocals, or vocal-free - while mismatched choices overlay one song's vocal on the other's instrumental.

• The web page announces the vocal-free append before and after the work, and warns that stem separation adds a few minutes.

• The full test suite (8 tests) stays green.

Iteration 17 (2026-08-07): Both-boxes-unchecked corrected to an instrumental BLEND, by owner clarification.

• The owner clarified the intended both-off behavior: step one, remove the vocals from both songs (Demucs); step two, BLEND the two now-instrumental tracks on top of each other into a single mash-up - no appending.

• Iteration 16's append-both-instrumentals behavior is reverted; appending is once again exclusive to both-boxes-checked. The blend uses the even instrumental gains from Iteration 13.

• The web page's messages now say "blending the two instrumentals into one vocal-free mash-up" before, and "Blended both instrumentals into one vocal-free mash-up!" after.

• The full test suite (8 tests) stays green.

Iteration 18 (2026-08-07): The third rectangle, the AIU Vocal to Instrumental Blender, by owner instruction.

• A third big rounded-corner rectangle sits below the first two, titled "AIU Vocal to Instrumental Blender" (renamed from "AIU Instrumental Blender" mid-build by the owner): one YouTube URL input with a Strip vocals & analyze button (spinner included), its own chaos slider with live value, and its own Play Forever and Stop buttons.

• The engine gained render_instrumental (Demucs strips ALL vocals from one song), and the web face gained the /instrumental route, which caches each song's instrumental by the source's content hash so stripping runs once per song.

• The stripped instrumental feeds the blender like any song - analyzed, arcs drawn, playable forever - and the third rectangle's buttons joined the shared mirrored button state, its slider steering the walk live when its Play Forever starts it.

• Proven end to end: a synthetic song posted to /instrumental answered HTTP 200 with a playable analyzed record; the full test suite (8 tests) stays green.

PLANNED REFINEMENTS (the resume attacks, in order)

• Learned beat embeddings: replace the hand-weighted MFCC-plus-chroma distance with a small contrastive model; the analysis-record interface does not change (the Architecture planned for this).

• Reinforcement Learning walk policy: reward smooth transitions and coverage, penalize stuck loops; replaces the inside of the walker only.

• Natural-language steering: map "keep it mellow" to chaos, energy bias, and section weights.

• Mashability model: learn compatibility scoring from rendered examples, replacing the rule-based tempo-and-Camelot score.

• Generative patching (stretch): synthesize short transitions where no good jump exists.


REFINEMENT REFLECTION

The wickedest problem so far is that "sounds seamless" is perceptual: the similarity threshold was set by ear on rendered walks, and the honest fix is the learned-embeddings refinement, which makes the human ear the training signal instead of a hand-tuned constant.

Iteration 2 taught a small interface lesson: a probability control without a visible number invites guessing; showing the value costs three lines and removes the guess.


END OF REFINEMENT VERSION 18
